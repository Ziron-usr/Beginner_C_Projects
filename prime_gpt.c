#include <stdio.h>


/* Write a program that takes a positive integer as input and checks if it is a prime number or not. 
A prime number is a number greater than 1 that is only divisible by 1 and itself. 
Print an appropriate message indicating whether the input is a prime number or not. */

int main(void)
{
    unsigned int input;

    
    default:
        break;
    }
}