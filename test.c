#include <stdio.h>
#include <string.h>

int main(void)
{
    char v2;
    char v1[v2];
    printf("Enter a value: ");
    scanf("%c", &v2);
    strlen(v2);

    printf("The ASCII value of %c is %d.\n", v1,v1);
    return 0; 
}
